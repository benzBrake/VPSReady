#!/usr/bin/env sh
# Git pre-commit hook: 自动为 .sh 文件添加可执行权限
# 配置方法: git config core.hooksPath .githooks

set -e

# 获取暂存区中新增或修改的 .sh 文件
SH_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.sh$' || true)

if [ -n "$SH_FILES" ]; then
    for FILE in $SH_FILES; do
        if [ -f "$FILE" ]; then
            echo "🔧 为 $FILE 添加可执行权限"
            git update-index --chmod=+x "$FILE"
        fi
    done
fi

exit 0
